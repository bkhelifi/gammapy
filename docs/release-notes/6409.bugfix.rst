Fixed an issue where ``UniformPrior`` and ``LogUniformPrior`` without explicit parameter ``min`` / ``max`` caused Minuit to hit infinite likelihood and fail. Parameter bounds now sync dynamically with the prior bounds.
